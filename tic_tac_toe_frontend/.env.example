# Copy this file to ".env" and fill in your values.
# Note: In Create React App, only variables prefixed with REACT_APP_ are exposed to the client.
# IMPORTANT: Although required for this demo, embedding API keys in client apps exposes them in the built bundle.
# Use with caution in production and prefer a backend proxy for secure usage.

REACT_APP_OPENAI_API_KEY=your_openai_api_key_here
